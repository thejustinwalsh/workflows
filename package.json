{
	"name": "workflows",
	"description": "Reusable GitHub Actions for automated changelogs and releases",
	"version": "1.0.0",
	"private": true,
	"repository": "thejustinwalsh/workflows",
	"workspaces": ["generate-changesets", "release", "filter-packages", "fixed-github-release"],
	"license": "MIT",
	"scripts": {
		"test": "node --test tests/*.test.mjs",
		"changeset": "changeset",
		"changeset:version": "changeset version"
	},
	"devDependencies": {
		"@changesets/cli": "^2.30.0",
		"@changesets/changelog-github": "^0.6.0"
	}
}
