# Changesets

This repo uses [changesets](https://github.com/changesets/changesets) for version management.

Changesets are auto-generated from conventional commits on PRs via our own `generate-changesets` action.
